name: cookgen
description: Synthesizes physically plausible cook fixtures (.smk/.mrk) from a thermal model.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
dependencies:
  args: ^2.6.0
  bridge_protocol:
    path: ../bridge_protocol
dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
