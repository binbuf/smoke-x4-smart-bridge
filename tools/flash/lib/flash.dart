/// T5 — merged-image builder, esp-web-tools manifest, and the documented
/// esptool command, all computed from the build's own `flasher_args.json`.
library;

export 'src/manifest.dart';
export 'src/merge.dart';
