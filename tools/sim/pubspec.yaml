name: sim
description: >-
  The fake bridge: serves the complete HTTP + WebSocket device API from recorded or
  synthesized cook data, with the full adverse-scenario matrix and mDNS advertising.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
dependencies:
  args: ^2.6.0
  bridge_protocol:
    path: ../bridge_protocol
  cookgen:
    path: ../cookgen
dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
