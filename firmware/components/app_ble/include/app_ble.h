/* app_ble.h — declared in the design 03 §3.1 component map; implemented in M1+.
 * The M0 skeleton keeps the component compiled and linked so the build
 * shape is real from the first commit. */
#ifndef APP_BLE_H
#define APP_BLE_H

#ifdef __cplusplus
extern "C" {
#endif

/* Returns 0 on success. Stub until the component's milestone lands. */
int app_ble_init(void);

#ifdef __cplusplus
}
#endif

#endif /* APP_BLE_H */
