/// Transport layer: `BridgeTransport` plus the Http/Ble/Mock implementations.
///
/// The UI never knows how it is talking to the bridge (design 08 §8.1).
/// The interface and implementations land with tasks A3.1–A3.3.
library;
