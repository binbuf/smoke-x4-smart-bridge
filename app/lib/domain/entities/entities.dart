/// Domain entities: Probe, Sample, CookSession, Mark, Alarm, BridgeStatus.
///
/// Pure Dart, ZERO Flutter imports — a hard rule enforced in CI (T1.6).
/// The entities themselves land with task A2.1. See design 08 §8.3.
library;
